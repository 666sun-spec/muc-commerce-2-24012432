# 第7天学生项目：电商用户分析Web系统

## 运行方法

```bash
python -m pip install -r requirements.txt
python app.py
```

浏览器访问 `http://127.0.0.1:5000`。

- 用户名：`student`
- 密码：`day07`

## 上午停止线

上午只完成以下内容：

1. 建立并重命名个人项目；
2. 从VS Code终端启动Flask；
3. 测试正确登录、错误登录、访问拦截和退出；
4. 定位`app.py`、`services`、`templates`、`static`和`data`；
5. 核对已有2张指标卡、1张图和“总用户数”问答；
6. 搜索TODO，但暂不修改`TODO 2-1`至`TODO 4-1`。

## 下午核心任务

1. 检查并解释登录、退出和登录访问控制；
2. 完成4张指标卡和2张真实图表；
3. 完成偏好品类筛选；
4. 完成至少4类离线规则问答；
5. 在`screenshots`目录保存4张核心验收截图；
6. 完成一项必选拓展；
7. 修改本文件，填写个人信息、完成功能、拓展说明和未解决问题。

使用编辑器搜索`TODO`即可找到需要完成的位置。

## 必选拓展（三选一）

### A. 导出当前筛选结果

新增CSV下载功能，导出内容必须与当前`category`筛选一致。

### B. 生命周期详情页

新增`/segments`页面，展示各生命周期阶段的用户数、流失人数和流失率。

### C. Flask自动化测试

新增`tests/`目录，至少测试正确登录、未登录访问看板和`/api/ask`。

完成后必须填写下面的拓展信息，并提供`screenshots/05_extension.png`、`test_result.txt`或`tests/`测试文件作为证据。

## 学生信息

- 姓名：孙博雯
- 学号：24012432
- 专题方向：A
- 已完成功能:5(除选做)
- 选择的拓展任务：A
- 拓展访问或运行方法：## 拓展A：导出当前筛选结果 一、功能介绍在数据看板页面增加导出功能，可将下拉框筛选后的品类数据导出为CSV文件，导出内容与页面表格展示数据完全一致；导出文件名携带对应筛选品类信息，同时对下载接口增加登录权限校验，未登录用户无法调用导出功能。二、接口访问方式1. 页面按钮使用（推荐）先使用账号`student`、密码`day07`登录系统，进入电商用户数据看板；在页面顶部下拉框选择需要筛选的品类，页面表格会自动刷新对应数据；点击右上角【导出当前品类筛选数据】按钮，浏览器会直接下载CSV文件。2. 浏览器地址直接访问接口路由地址：`/download`请求传参格式：`/download?category=品类名称`示例：导出Fashion品类筛选数据：`http://127.0.0.1:5000/download?category=Fashion`导出全部品类数据：`http://127.0.0.1:5000/download?category=全部`未登录直接访问该地址会自动重定向至登录页面。三、导出文件规则1. 数据一致性：导出CSV的字段、数据条目和页面当前筛选表格严格对应；2. 文件命名：选择「全部」时文件名为`全部_用户品类分析表.csv`，选择单个品类时文件名为`对应品类_用户品类分析表.csv`；3. 编码格式：CSV文件使用`utf-8-sig`编码，使用Excel打开不会出现中文乱码；4. 容错处理：若筛选后无匹配数据，接口返回文字提示，不会生成空白CSV文件。### 四、项目修改内容1. 后端`app.py`新增`/download`下载路由函数`download_csv`；补充导入`pandas`、`io`、`send_file`依赖库；路由添加`@login_required`登录鉴权装饰器；复用项目原有`load_dashboard_data`函数复用筛选逻辑，保证前后端筛选条件统一。2. 前端`templates/dashboard.html`
在页面右上角按钮区域新增导出按钮，绑定`url_for`路由并携带当前选中品类参数。五、成果截图归档将点击导出按钮后的浏览器下载弹窗截图、CSV文件打开预览截图，放置在项目`screenshots`文件夹中，文件命名为`05_extension_A.png`留存归档。
- 拓展证据文件：05_extension.png
- 尚未解决的问题：0
